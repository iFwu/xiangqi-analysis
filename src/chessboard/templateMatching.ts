import { PieceType, PieceColor } from './types';

export function preprocessAllTemplates(): Record<PieceType, ImageData> {
  // 实现预处理所有模板图像的逻辑
  return {} as Record<PieceType, ImageData>;
}

export function templateMatchingForPiece(
  processedPiece: ImageData,
  templates: Record<PieceType, ImageData>,
  pieceColor: PieceColor
): PieceType | null {
  // 实现模板匹配逻辑
  return null;
}